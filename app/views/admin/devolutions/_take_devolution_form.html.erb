<div class="row pb-3">
  <div class="col-12">
    <%= form_for [:admin, devolution] do |f| %>
      <%= f.hidden_field :user_id, value: current_user.id %>
      <%= f.submit t('labels.take'), class: 'btn btn-secondary btn-lg' %>
    <% end if can? :take, devolution %>
  </div>
</div>
