<!-- ============================================================== -->
<!-- left sidebar -->
<!-- ============================================================== -->
<div class="nav-left-sidebar sidebar-dark">
  <div class="menu-list">
    <nav class="navbar navbar-expand-lg navbar-light">
      <a class="d-xl-none d-lg-none" href="#">Dashboard</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav flex-column">
          <li class="nav-divider">
            Menu
          </li>

          <li class="nav-item">
            <%= link_to admin_dashboard_index_path,
              class: "nav-link #{active_class_for('admin/dashboard')}" do %>
            <i class="fas fa-fw fa-th"></i>
            <%= t('labels.dashboard') %>
            <% end %>
          </li>

          <li class="nav-item">
            <%= link_to admin_users_path,
              class: "nav-link #{active_class_for('admin/users')}" do %>
            <i class="fas fa-users"></i>
            <%= User.model_name.human(count: 2) %>
            <% end if can? :read, User %>
          </li>

          <li class="nav-item">
            <%= link_to admin_orders_path,
              class: "nav-link #{active_class_for('admin/orders')}" do %>
            <i class="fas fa-book"></i>
            <%= Order.model_name.human(count: 2) %>
            <% end if can? :read, Order %>
          </li>

          <li class="nav-item">
            <%= link_to session_path(current_user),
              class: "nav-link", method: :delete do %>
            <i class="fas fa-fw fa-power-off mr-2"></i>
            <%= t('labels.sign_out') %>
            <% end %>
          </li>
        </ul>
      </div>
    </nav>
  </div>
</div>
<!-- ============================================================== -->
<!-- end left sidebar -->
<!-- ============================================================== -->
